import React from 'react';

export const Table = ({columns, data}) => (

    <ul>
        data.map();
    </ul>

    /*<table>

        <thead>
            columns.map((column) => {
                <th>
                    column.header
                </th>
            });
        </thead>

        <tbody>
        columns.map((column) => {
            <th>
                En verdi
            </th>
        });
        </tbody>

    </table>*/

);
